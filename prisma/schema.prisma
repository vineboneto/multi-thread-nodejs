generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model tbl_temp {
  id_temp Int    @id @default(autoincrement())
  nome    String
}
